# Simple Chat — lightweight conversational AI, no tools
# Copy to ~/.grandpa/config.toml
#
# The fastest setup: just Ollama + a model.
#
# Usage:
#   grandpa ask "What is quantum computing?"
#   grandpa chat

[engine]
default = "ollama"

[engine.ollama]
host = "http://127.0.0.1:11434"

[intelligence]
default_model = "grandpa-mini:latest"  # Default and lowest resource use
# default_model = "grandpa-fast:latest"  # Better general responses
# default_model = "llama3.1:8b"    # Alternative model

[agent]
default_agent = "simple"            # Single-turn, no tools

[server]
host = "127.0.0.1"
port = 8000
