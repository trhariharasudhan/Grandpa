# Models

This directory contains Ollama Modelfile recipes only.

Examples include:

- `grandpa-brain`
- `grandpa-fast`
- `grandpa-coder`
- `grandpa-eyes`
- `grandpa-guard`

No model weights are stored in this repository. Pull or build runtime model
artifacts through Ollama or the documented local setup flow instead of
committing generated model files.
