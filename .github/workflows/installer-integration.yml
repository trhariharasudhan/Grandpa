name: Installer integration

on:
  pull_request:
    paths:
      - 'scripts/install/**'
      - 'src/grandpa/cli/**'
      - 'tests/install/**'
      - '.github/workflows/installer-integration.yml'
  schedule:
    - cron: '0 6 * * *'

jobs:
  container-matrix:
    name: ${{ matrix.image }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        image:
          - ubuntu:22.04
          - ubuntu:24.04
          - fedora:40

    container: ${{ matrix.image }}

    steps:
      - name: Install prereqs (Ubuntu/Debian)
        if: contains(matrix.image, 'ubuntu') || contains(matrix.image, 'debian')
        run: |
          apt-get update
          apt-get install -y curl git python3 sudo

      - name: Install prereqs (Fedora)
        if: contains(matrix.image, 'fedora')
        run: |
          dnf install -y curl git python3 sudo

      - name: Create non-root user
        run: useradd -m -s /bin/bash testuser

      - uses: actions/checkout@v4
        with:
          path: grandpa-src

      - name: Install Ollama mock
        run: install -m 755 "$GITHUB_WORKSPACE/grandpa-src/tests/install/bash/stubs/ollama-mock" /usr/local/bin/ollama

      - name: Run installer
        run: |
          chown -R testuser:testuser /home/testuser "$GITHUB_WORKSPACE/grandpa-src"
          su testuser -c '
            export GRANDPA_REPO_URL=file://'"$GITHUB_WORKSPACE"'/grandpa-src
            cd '"$GITHUB_WORKSPACE"'/grandpa-src
            bash scripts/install/install.sh --no-bg-orchestrator
          '

      - name: Verify install state
        run: |
          su testuser -c '
            test -d ~/.grandpa/src
            test -d ~/.grandpa/.venv
            test -f ~/.grandpa/config.toml
            test -f ~/.grandpa/.state/install-state.json
            test -L ~/.local/bin/grandpa
          '

      - name: Verify grandpa --version
        run: su testuser -c '~/.local/bin/grandpa --version'

      - name: Verify grandpa doctor exits 0
        run: su testuser -c '~/.local/bin/grandpa doctor'

      - name: Verify uninstall is clean
        run: |
          su testuser -c '
            ~/.local/bin/grandpa-uninstall
            test ! -d ~/.grandpa
            test ! -L ~/.local/bin/grandpa
          '

  macos:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14, macos-15]

    steps:
      - uses: actions/checkout@v4

      - name: Install Ollama mock
        run: sudo install -m 755 tests/install/bash/stubs/ollama-mock /usr/local/bin/ollama

      - name: Run installer
        run: |
          export GRANDPA_REPO_URL=file://$(pwd)
          bash scripts/install/install.sh --no-bg-orchestrator

      - name: Verify install state
        run: |
          test -d ~/.grandpa/src
          test -d ~/.grandpa/.venv
          test -f ~/.grandpa/config.toml
          test -L ~/.local/bin/grandpa

      - name: Verify grandpa --version
        run: ~/.local/bin/grandpa --version

      - name: Verify grandpa doctor exits 0
        run: ~/.local/bin/grandpa doctor

      - name: Verify uninstall is clean
        run: |
          ~/.local/bin/grandpa-uninstall
          test ! -d ~/.grandpa
          test ! -L ~/.local/bin/grandpa
