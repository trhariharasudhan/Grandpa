[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "grandpa-rust"
version = "0.1.0"
description = "Rust extension module for grandpa"
requires-python = ">=3.10"

[tool.maturin]
manifest-path = "Cargo.toml"
