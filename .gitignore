# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg

# Virtual environments
.venv/
/.venv311/
/Grandpa/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# uv
.uv-cache/
# Note: uv.lock is intentionally tracked — it pins all transitive deps for reproducibility.
# Uncomment the line below only if you deliberately choose not to commit the lock file.
# uv.lock

# OS
.DS_Store
Thumbs.db

# Secrets
.env
.env.*
!.env.example

# Project
*.sqlite
*.db
*.jsonl
*.npz
*.log
runtime/reports/
runtime/burnin/
runtime/**/*.jsonl
runtime/**/*.db
runtime/**/*.sqlite
results/
logs/
traces/
coding_task_*
get-pip.py
# Junk from mocked-path tests that write to their mock's __repr__ as a path
MagicMock/

# Auto-generated tree dump
TREE_STRUCTURE.md

# MkDocs build output
site/

# Worktrees
.worktrees/

# Rust
target/

# Claude plan artifacts
docs/plans/
docs/superpowers/
.superpowers/

# Claude Code project instructions (per-developer)
CLAUDE.md
.claude/

# NFS lock artifacts
.nfs*
**/.nfs*

# Research output
research_mining_*
.python-version
src/grandpa/channels/whatsapp_baileys_bridge/node_modules/

# SQLite in-memory artifacts
:memory:

# Dogfood reports (regenerated locally; not for VCS)
dogfood_report*.md

# Second Repos
Inline/
scratch/

# ---------------------------------------------------------------------------
# Distillation runtime artifacts (defense in depth — these should always live
# in ~/.grandpa/, never inside the source tree, but we ignore them here in
# case GRANDPA_HOME is misconfigured during dev)
# ---------------------------------------------------------------------------
.grandpa/
learning.db
**/learning/sessions/
**/learning/pending_review/
**/learning/benchmarks/
**/teacher_traces/
*.session.json

# Local runtime data
runtime/

# Local cloned-voice runtime artifacts
voice_runtime/.venv/
voice_runtime/models_or_cache/
voice_runtime/outputs/
voice_runtime/logs/
voice_runtime/references/*
!voice_runtime/references/reference.example.toml
voice_runtime/**/*.ckpt
voice_runtime/**/*.pt
voice_runtime/**/*.pth
voice_runtime/**/*.safetensors

# Personal voice-training datasets (scripts remain reviewable)
voice_runtime/datasets/*/raw/
voice_runtime/datasets/*/processed/
voice_runtime/datasets/*/wavs/
voice_runtime/datasets/*/metadata/
voice_runtime/datasets/*/reports/
